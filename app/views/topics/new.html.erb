<h1>New Topic</h1>

<div class="row">
  <div class="col-md-r">
    <p>Guidelines for topics:</p>
    <ul>
      <li>Make sure the topic is dope.</li>
      <li>Never question your first idea.</li>
      <li>Never use your first idea.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for @topic do |f| %>
      <div class="form-group">
        <%= f.label :name %>
        <%= f.text_field :name, class: 'form-control', placeholder: "Topic name!" %>
      </div>
      <div class="form-group">
        <%= f.label :description %>
        <%= f.text_area :description, rows: 5, class: 'form-control', placeholder: "Write your boldest idea here." %>
      </div>
      <div class="form-group">
        <%= f.label :public, class: 'checkbox' do %>
          <%= f.check_box :public %> Public Topic
        <% end %>
      </div>
      <%= f.submit "Save", class: 'btn btn-success' %>
    <% end %>
  </div>
</div>