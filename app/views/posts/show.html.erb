<h1><%= @post.markdown_title %></h1>

<div class="row">
  <div class="col-md-8">
    <small>
      <%= image_tag(@post.user.avatar.tiny.url) if @post.user.avatar? %>
      submitted <%= time_ago_in_words(@post.created_at) %> ago by
      <%= @post.user.name %></p>
    </small>
    <% if policy(@post).edit? %>
      <%= link_to "Edit", edit_topic_post_path(@topic, @post), class: 'btn btn-success' %></p>
    <% end %>
    <p><%= markdown @post.body %></p>
    <p><%= image_tag(@post.postimage.small.url) %></p>

    <%= render file: "/Users/Damo/code/bloccit/app/views/comments/_form", locals: { topic: @topic, post: @post, comment: @comment } %>

    <%= render file: "/Users/Damo/code/bloccit/app/views/comments/_comment" %>

  </div>
</div>
